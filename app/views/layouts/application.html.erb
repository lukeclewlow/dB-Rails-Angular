<!DOCTYPE html>
<html ng-app="dbApp">
	<head>
	  <title>Db2</title>
	  <%= stylesheet_link_tag    'application', media: 'all' %>
	  <%= javascript_include_tag 'application', controller_name %>
	  <%= csrf_meta_tags %>
	</head>
	<body>
		<container ng-controller="LayoutCtrl as layoutCtrl">
			<nav class="navbar-fixed-top header-center">		
				<ul class="nav nav-pills ">
					<a href="#"><h1 class="logo">d(-_-)b</h1></a>
			    <li class="dropdown pull-right">
			       <button data-toggle="dropdown" class="dropdown-toggle">Genres<b class="caret"></b></button>
			        <ul class="dropdown-menu">
			        	<container ng-repeat="genre in layoutCtrl.genres">
				          <li><a href="#/{{genre.id}}">{{genre.name}}</a></li>
				        </container>
			        </ul>
			    </li>
				</ul>

			</nav>
		</container>
		<p class="notice"><%= notice %></p>
		<p class="alert"><%= alert %></p>
		<%= yield %>

	</container>
	</body>
</html>
