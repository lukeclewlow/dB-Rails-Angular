<div ng-controller="MainCtrl as ctrl">
	<h1 ng-show="ctrl.tunes.length == 0">Nothing here yet</h1>
	<ul ng-show="ctrl.tunes" ng-repeat="tune in ctrl.tunes">
		<li>{{tune.artist}}</li>
		<li>{{tune.title}}</li>
		<li>{{tune.link}}</li>
	</ul>
</div>